# Sample .env project file.  Copy to [project_root]/.env, then edit the values
# as required for your setup.

# The full path to your cod install
COD_PATH='/home/mark/.wine/drive_c/Program Files (x86)/Activision/Call of Duty 4 - Modern Warfare'

# The full path where the compiled code will reside
MOD_PATH='/home/mark/.wine/drive_c/Program Files (x86)/Activision/Call of Duty 4 - Modern Warfare/Mods/rotudev'

# The full path to a convienent folder for ftp'ing the server files to the server
UPLOAD_PATH='/home/mark/Desktop/rotudevUpload'

# The full path to a convenient folder for packaging a release
RELEASE_PATH='/home/mark/Desktop'

# The full path to a your git working copy src folder. Only needed to package releases
WORK_PATH='/home/mark/RotU.repo.recovery/rotu.google.code/reign-of-the-undead/trunk/src'

# If you add/remove config files, update this array so makeMod.pl works properly!
CONFIG_FILES=admin.cfg,damage.cfg,didyouknow.cfg,easy.cfg,mapvote.cfg,server.cfg,game.cfg,startnewserver.cfg,weapons.cfg                

# Build type.  Either [DEBUG | DEPLOY].  Typically, DEBUG for local testing/development, or DEPLOY
# for deploying to a LAN or Internet server. 
BUILD_TARGET=DEBUG

