*.cfg files placed here will override the default versions from src/,
and will be copied into build/ and into your modpath.

If you copy the *.cfg files from build/ here, and modify them, your
custom settings will be pushed with the rebuilt mod.  You should still keep
a backup copy of your *.cfg files elsewhere (like a separate local git).

** Not Implemented Yet **
